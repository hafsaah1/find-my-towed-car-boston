SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
# anon (public) key — safe to expose in frontend
SUPABASE_ANON_KEY=
# service_role (secret) key — server-side only, used by enrich.py + GitHub Actions
SUPABASE_SERVICE_KEY=
